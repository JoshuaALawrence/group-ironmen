title = "group-ironmen gitleaks config"

[extend]
# Use the default gitleaks rules as the base
useDefault = true

[allowlist]
description = "Global allowlist"
paths = [
    '''\.env\.example$''',
    '''package-lock\.json$''',
    '''Cargo\.lock$''',
    '''\.sarif$''',
]
